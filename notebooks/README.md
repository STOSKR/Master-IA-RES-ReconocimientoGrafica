# Notebooks

Espacio para demos academicas. El flujo recomendado es cargar una captura, mostrar cada fase del pipeline y comparar la salida con ground truth.

El codigo productivo vive en `src/recongrafica`; los notebooks deben importarlo en lugar de duplicar logica.
